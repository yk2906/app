[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "notion-client>=3.0.0",
    "python-dotenv>=1.2.2",
    "slack-sdk>=3.41.0",
]
